name: Auto PR Staging to Main

on:
  schedule:
    # This is 9:00 UTC (fixed) - converts to 2:00 AM MST (UTC-7) and 3:00 AM MDT (UTC-6)
    - cron: "0 9 * * 1,3" # Monday and Wednesday
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

env:
  BRANCH_FROM: staging
  BRANCH_TO: main

jobs:
  create-pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Check for changes between staging and main
        id: diff
        run: |
          echo "🔍 Checking for new commits in 'staging' not present in 'main'..."
          git fetch origin staging
          git fetch origin main
          COMMITS_AHEAD=$(git rev-list --count origin/main..origin/staging)
          echo "Commits ahead: $COMMITS_AHEAD"

          if [ "$COMMITS_AHEAD" -eq 0 ]; then
            echo "✅ No new commits to merge from 'staging' to 'main'. Exiting gracefully."
            echo "no_changes=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Check if there are changes in application directories
          APP_CHANGES=$(git diff --name-only origin/main..origin/staging | grep -E "^(api|web-app|core)/" || true)

          if [ -z "$APP_CHANGES" ]; then
            echo "ℹ️ No application changes found between staging and main branches. Skipping PR creation."
            echo "no_changes=true" >> $GITHUB_OUTPUT
          else
            echo "🚀 Application changes detected. Proceeding to create PR."
            echo "Application changes found:"
            echo "$APP_CHANGES"
            echo "no_changes=false" >> $GITHUB_OUTPUT

            # Determine which apps have changes
            CHANGED_APPS=""
            if echo "$APP_CHANGES" | grep -q "^api/"; then
              CHANGED_APPS="${CHANGED_APPS}API, "
            fi
            if echo "$APP_CHANGES" | grep -q "^web-app/"; then
              CHANGED_APPS="${CHANGED_APPS}Web App, "
            fi
            if echo "$APP_CHANGES" | grep -q "^core/"; then
              CHANGED_APPS="${CHANGED_APPS}Core, "
            fi
            # Remove trailing comma and space
            CHANGED_APPS=$(echo "$CHANGED_APPS" | sed 's/, $//')
            echo "changed_apps=$CHANGED_APPS" >> $GITHUB_OUTPUT
          fi

      - name: Debug - Log no_changes value
        run: |
          echo "no_changes value: ${{ steps.diff.outputs.no_changes }}"
          if [ "${{ steps.diff.outputs.no_changes }}" == "true" ]; then
            echo "✅ no_changes is TRUE - PR creation will be skipped"
          else
            echo "🚀 no_changes is FALSE - PR creation will proceed"
          fi

      - name: Log environment info
        if: steps.diff.outputs.no_changes == 'false'
        run: |
          echo "GitHub ref: $GITHUB_REF"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Actor: $GITHUB_ACTOR"
          echo "Workspace: $GITHUB_WORKSPACE"
          echo "Event name: $GITHUB_EVENT_NAME"

      - name: Get commit authors
        id: get_authors
        if: steps.diff.outputs.no_changes == 'false'
        uses: actions/github-script@v8
        with:
          script: |
            const compare = await github.rest.repos.compareCommits({
              owner: context.repo.owner,
              repo: context.repo.repo,
              base: process.env.BRANCH_TO,
              head: process.env.BRANCH_FROM,
            });

            const commits = compare.data.commits;

            // Get all unique author logins
            const logins = [
              ...new Set(commits.map(c => c.author?.login).filter(Boolean))
            ];

            core.info(`Found ${commits.length} total commits.`);
            core.info(`Contributors (usernames): ${logins.join(', ')}`);
            core.setOutput('reviewers', logins.join(','));

      - name: Check if PR already exists
        if: steps.check_changes.outputs.has_changes == 'true'
        id: check_pr_exists
        run: |
          # Check for existing open PR from staging to main
          EXISTING_PR=$(gh pr list --base staging --head main --state open --json number --jq '.[0].number')
          if [ -n "$EXISTING_PR" ]; then
            echo "pr_exists=true" >> $GITHUB_OUTPUT
            echo "pr_number=$EXISTING_PR" >> $GITHUB_OUTPUT
            echo "ℹ️ PR #$EXISTING_PR already exists from staging to main"
          else
            echo "pr_exists=false" >> $GITHUB_OUTPUT
            echo "No existing PR found from staging to main"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create PR
        id: create_pr
        if: steps.diff.outputs.no_changes == 'false'
        uses: actions/github-script@v8
        with:
          result-encoding: string
          script: |
            const { data: pr } = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              base: process.env.BRANCH_TO,
              head: process.env.BRANCH_FROM,
              title: `Merge changes from ${process.env.BRANCH_FROM} into ${process.env.BRANCH_TO}`,
              body: `This PR merges application changes (${{ steps.diff.outputs.changed_apps }}) from \`${process.env.BRANCH_FROM}\` into \`${process.env.BRANCH_TO}\`. Contributors are assigned as reviewers. Please review your changes carefully and approve if appropriate. If all reviewers approve, this PR will be merged automatically this evening.`,
              draft: false,
            });

            core.info(`Created PR #${pr.number}: ${pr.html_url}`);
            core.setOutput('pr_number', pr.number.toString());

      - name: Assign reviewers
        if: steps.diff.outputs.no_changes == 'false' && steps.get_authors.outputs.reviewers != '' && steps.create_pr.outputs.pr_number != ''
        uses: actions/github-script@v8
        with:
          script: |
            const reviewers = '${{ steps.get_authors.outputs.reviewers }}'
              .split(',')
              .filter(Boolean);
            const prNumber = Number('${{ steps.create_pr.outputs.pr_number }}');

            core.info(`Assigning reviewers: ${reviewers.join(', ')} to PR #${prNumber}`);

            if (reviewers.length > 0) {
              await github.rest.pulls.requestReviewers({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber,
                reviewers,
              });

              core.info('Reviewers successfully assigned.');
            } else {
              core.info('No reviewers to assign.');
            }

      - name: Log no changes found
        if: steps.diff.outputs.no_changes == 'true'
        run: echo "No application changes to merge. Workflow completed without creating a PR."
