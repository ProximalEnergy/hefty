name: Deploy API to Elastic Beanstalk

on:
  push:
    branches:
      - staging
      - main
      - sandbox
    paths:
      - "api/**"
      - ".github/workflows/api-deploy.yml"
      - ".github/actions/install-hurl/**"
  workflow_dispatch:
    inputs:
      force_deploy:
        description: "Force deploy even without API changes"
        required: false
        default: false
        type: boolean
  workflow_run:
    workflows:
      - "Auto Merge Staging to Main if All Reviewers Approved"
      - "Publish Core to AWS CodeArtifact"
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Only run if core-publish succeeded (when triggered by workflow_run from core-publish)
    if: |
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.name != 'Publish Core to AWS CodeArtifact' ||
      github.event.workflow_run.conclusion == 'success'
    outputs:
      branch_name: ${{ steps.extract_branch.outputs.branch_name }}

    steps:
      - uses: actions/checkout@v4

      - name: Log event context
        run: |
          echo "Event name: ${{ github.event_name }}"
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "Triggered by workflow_run."
            echo "workflow_run head_branch: ${{ github.event.workflow_run.head_branch }}"
          else
            echo "Triggered by push or other event."
            echo "github.ref_name: ${{ github.ref_name }}"
          fi

      - name: Extract branch name
        id: extract_branch
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "BRANCH_NAME=${{ github.event.workflow_run.head_branch }}" >> $GITHUB_ENV
            echo "branch_name=${{ github.event.workflow_run.head_branch }}" >> $GITHUB_OUTPUT
            echo "Extracted branch name from workflow_run: ${{ github.event.workflow_run.head_branch }}"
          else
            echo "BRANCH_NAME=${{ github.ref_name }}" >> $GITHUB_ENV
            echo "branch_name=${{ github.ref_name }}" >> $GITHUB_OUTPUT
            echo "Extracted branch name from ref_name: ${{ github.ref_name }}"
          fi

      - name: Set environment variables
        run: |
          echo "Setting environment variables for branch: $BRANCH_NAME"
          if [ "$BRANCH_NAME" = "main" ]; then
            echo "ENVIRONMENT=production" >> $GITHUB_ENV
            echo "ENV_NAME=${{ vars.PROD_ELASTIC_BEANSTALK_ENV_NAME }}" >> $GITHUB_ENV
            echo "VERSION_PREFIX=production" >> $GITHUB_ENV
            echo "Set production environment variables."
          elif [ "$BRANCH_NAME" = "staging" ]; then
            echo "ENVIRONMENT=staging" >> $GITHUB_ENV
            echo "ENV_NAME=${{ vars.STAGING_ELASTIC_BEANSTALK_ENV_NAME }}" >> $GITHUB_ENV
            echo "VERSION_PREFIX=staging" >> $GITHUB_ENV
            echo "Set staging environment variables."
          elif [ "$BRANCH_NAME" = "sandbox" ]; then
            echo "ENVIRONMENT=sandbox" >> $GITHUB_ENV
            echo "ENV_NAME=${{ vars.SANDBOX_ELASTIC_BEANSTALK_ENV_NAME }}" >> $GITHUB_ENV
            echo "VERSION_PREFIX=sandbox" >> $GITHUB_ENV
            echo "Set sandbox environment variables."
          else
            echo "Branch '$BRANCH_NAME' is not configured for deployment. Skipping deployment."
            echo "SKIP_DEPLOYMENT=true" >> $GITHUB_ENV
          fi

      - name: Install uv
        if: env.SKIP_DEPLOYMENT != 'true'
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: set up python
        if: env.SKIP_DEPLOYMENT != 'true'
        run: uv python install
        working-directory: api

      - name: Copy core library source into API directory
        if: env.SKIP_DEPLOYMENT != 'true'
        run: |
          # Copy core source code directly into api directory
          mkdir -p api/core
          cp -r core/src/core/* api/core/
          echo "Core library copied to api/core"
          ls -la api/core

      - name: Generate requirements.txt without core
        if: env.SKIP_DEPLOYMENT != 'true'
        run: |
          # Generate requirements excluding the workspace core package
          uv lock
          uv export --frozen --no-emit-workspace --no-dev --no-editable -o requirements.txt --no-hashes

          # Remove ALL references to core from requirements.txt since we're bundling it directly
          # Use grep to filter out lines starting with "core" and comment lines mentioning core
          grep -v "^core[=@]" requirements.txt > requirements.tmp && mv requirements.tmp requirements.txt
          grep -v "^[[:space:]]*#.*via core$" requirements.txt > requirements.tmp && mv requirements.tmp requirements.txt

          echo "Generated requirements.txt without core dependency"
          echo ""
          echo "Checking for any remaining core references:"
          if grep -i "^core" requirements.txt; then
            echo "WARNING: Core references still found in requirements.txt!"
            exit 1
          else
            echo "✓ No core references found - good!"
          fi
          echo ""
          echo "Verifying core directory structure:"
          ls -la core/
          echo ""
          echo "Checking core __init__.py exists:"
          if test -f core/__init__.py; then
            echo "✓ core/__init__.py found"
          else
            echo "ERROR: core/__init__.py not found!"
            exit 1
          fi
        working-directory: api

      - name: Generate deployment package
        if: env.SKIP_DEPLOYMENT != 'true'
        run: |
          cd api
          # Verify structure before zipping
          echo "Files to be included in deployment:"
          echo "- requirements.txt: $(test -f requirements.txt && echo 'EXISTS' || echo 'MISSING')"
          echo "- app directory: $(test -d app && echo 'EXISTS' || echo 'MISSING')"
          echo "- core directory: $(test -d core && echo 'EXISTS' || echo 'MISSING')"
          echo "- Procfile: $(test -f Procfile && echo 'EXISTS' || echo 'MISSING')"
          echo ""
          # Create deployment package with bundled core (.ebignore will handle exclusions)
          zip -r ../deploy.zip . -x "*.git*"
          echo ""
          echo "Deployment package created"
          cd ..
          echo ""
          echo "Verifying critical files in package:"
          echo "=== requirements.txt ==="
          unzip -l deploy.zip | grep "requirements.txt" || echo "ERROR: requirements.txt not found in package!"
          echo ""
          echo "=== core module files (first 20) ==="
          unzip -l deploy.zip | grep "core/" | head -20 || echo "ERROR: core directory not found in package!"
          echo ""
          echo "=== app module files (first 20) ==="
          unzip -l deploy.zip | grep "app/" | head -20
          echo ""
          echo "=== Package size ==="
          ls -lh deploy.zip

      - name: echo
        run: |
          echo "${{ env.VERSION_PREFIX }}-${{ github.sha }}-${{ github.run_number }}"
          echo ${{ secrets.AWS_S3_REGION }}

      - name: Deploy to Elastic Beanstalk
        if: env.SKIP_DEPLOYMENT != 'true'
        uses: einaregilsson/beanstalk-deploy@v22
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          application_name: api
          environment_name: ${{ env.ENV_NAME }}
          version_label: "${{ env.VERSION_PREFIX }}-${{ github.sha }}-${{ github.run_number }}"
          region: ${{ secrets.AWS_S3_REGION }}
          deployment_package: deploy.zip
          wait_for_deployment: true

      - name: Skip deployment
        if: env.SKIP_DEPLOYMENT == 'true'
        run: echo "Deployment skipped for branch '${{ env.BRANCH_NAME }}'"

  smoke-test:
    runs-on: ubuntu-latest
    needs: deploy
    if: needs.deploy.result == 'success'

    steps:
      - uses: actions/checkout@v4

      - name: Set environment variables for smoke testing
        run: |
          echo "BRANCH_NAME=${{ needs.deploy.outputs.branch_name }}" >> $GITHUB_ENV
          if [ "${{ needs.deploy.outputs.branch_name }}" = "main" ]; then
            echo "BASE_URL=${{ secrets.PROD_BASE_URL }}" >> $GITHUB_ENV
            echo "API_KEY=${{ secrets.PROD_API_KEY }}" >> $GITHUB_ENV
          elif [ "${{ needs.deploy.outputs.branch_name }}" = "staging" ]; then
            echo "BASE_URL=${{ secrets.STAGING_BASE_URL }}" >> $GITHUB_ENV
            echo "API_KEY=${{ secrets.STAGING_API_KEY }}" >> $GITHUB_ENV
          elif [ "${{ needs.deploy.outputs.branch_name }}" = "sandbox" ]; then
            echo "BASE_URL=${{ secrets.SANDBOX_BASE_URL }}" >> $GITHUB_ENV
            echo "API_KEY=${{ secrets.STAGING_API_KEY }}" >> $GITHUB_ENV
          else
            echo "No smoke test configuration for branch '${{ needs.deploy.outputs.branch_name }}'"
            exit 1
          fi

      - name: Install Hurl
        uses: ./.github/actions/install-hurl

      - name: Run smoke tests
        run: hurl --location --variable baseURL=${{ env.BASE_URL }} --variable apiKey=${{ env.API_KEY }} --test ./_smoke_testing/hurl/get/*
        working-directory: api
