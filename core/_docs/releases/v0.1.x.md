# v0.1.5

- Adds `is_commissioning` column in `operational.projects` in `models.py`

# v0.1.6

- Adds `data_receive_schedule` column in `operational.projects` in `models.py`

# v0.1.7

- Makes `polygon` column in `operational.projects` in `models.py` nullable

# v0.1.15

- Makes `name_scada` column in `operational.projects` in `models.py` not nullable and unique

# v0.1.16

- Removes `vector_store_id` column in `operational.projects` in `models.py`

# v0.1.17

- Adds performance modeling columns to `operational.rackings` in `models.py`

# v0.1.18

- Adds columns to `operational.rackings` for 2P trackers in `models.py`

# v0.1.19

- Adds `user_project_settings` to the `admin` schema

# v0.1.20

- Adds `bifaciality_factor` to `operational.pv_modules`
- Adds `cells_in_parallel` to `operational.pv_modules`

# v0.1.21

- Adds `drone_permissions`, `drone_integrations` and `drone_providers` to `operational` schema

# v0.1.22

- Adds `<project>.drone_inspections` and `<project>.drone_anomalies` to `<project>` schemas

# v0.1.23

- Removed `<project>.drone_inspections.project_id` from `<project>` schemas
