# v0.2.75

- Add `issues` table to project schemas for tracking device issues

# v0.2.74

- Add `.nullslast()` to order_by on Event homepage summary

# v0.2.73

- Add `get_status_timeseries_python` to core layer

# v0.2.72

- Add `ReportType` enum

# v0.2.71

- Add `KPIType` and `EventLossType` enums

# v0.2.70

- Add `get_event_losses_aggregated` function to `project.event_losses` CRUD for efficient aggregation of event losses by event_id with SQL-level grouping, significantly improving performance for waterfall calculations

# v0.2.69

- Add `include_underperformance` arg to `events.get_windowed_events`. Default = True, which persists old behavior.

# v0.2.68

- Narrow `devices.polygon` column to `MULTIPOLYGON` type

# v0.2.67

- Added functionality to facilitate data browser:
  - Minor fix to pivot function
  - Add `device_type_id` as an available parameter to `get_project_tags`
  - Add `get_tags_by_regex` function

# v0.2.66

- Restore `device_model_id` column to `devices` table

# v0.2.65

- Add event_messages, event_message_images, event_message_reactions, event_chat_mutes tables and added column on user_subscriptions

# v0.2.64

- Added ProjectType enum

# v0.2.63

- Specify `native_enum=False` for `data_interval` in `operational.projects` table

# v0.2.62

- Added / edited sensor type enums

# v0.2.61

- Add PV_EXPECTED_POWER as a sensor type and enum

# v0.2.60

- Add event homepage summary which utilizes new `loss_daily_financial` for significant homepage speedup

# v0.2.59

- Add loss_daily_financial to events tables

# v0.2.58

- Allow ModelList to host async queries
- Refactor some functions to return ModelList instead of Result

# v0.2.57

- Change QSE integrations to not enforce permissions in the core layer
- Split QSE permissions retrieval to a separate function

# v0.2.56

- Add `__str__`s for usage in sql-admin.

# v0.2.55

- Add interactions with QSE tables
- Add `model_list_to_pandas` util function

# v0.2.54

- Add `qse_fields` table for tracking third-party data naming

# v0.2.53

- Add QSE tables, similar in structure to CMMS tables
  - qse_integrations
  - qse_providers
  - qse_permissions

# v0.2.52

- Add `update_project_spec()` function to `operational.projects` CRUD for updating project spec JSON
- Add `get_unique_sensor_type_ids_from_tags()` function to `project.tags` CRUD for retrieving unique sensor type IDs from project tags
- Functions support automatic project spec updates when sensor types are assigned to tag patterns

# v0.2.51

- Add several crud functions for inserting, updating, and upserting event tables
- Add get_failure_modes crud
- Updates to model list (does not break functionality)
  - dtype normalizer for consistency and reduced failure
  - .pandas_dataframe is DB-first and faster in memory
  - .find() prefers SQL pushdown even with loaded items
  - Minor cleanup

# v0.2.50

- Update test

# v0.2.49

- Added async LRU cache dependency for dependencies

# v0.2.48

- Remove old test notebook

# v0.2.47

- Add `event_id` column to `drone_anomalies` table with foreign key to `events` table

# v0.2.46

- Remove unique constraint on ("device_id", "time_start") in `events` tables

# v0.2.44

- Allow nulls in `operational.pv_budgeted_data.ghi`, `temperature`, and `soiling_percentage` columns

# v0.2.43

- Add `device_model_id` to `<project>.devices` tables

# v0.2.42

- Add `operational.pv_budgeted_series` and `operational.pv_budgeted_data` tables

# v0.2.41

- Add tables `events_2` and `event_losses_2` for Event refactor

# v0.2.40

- Minor patch for polars pivot util function

# v0.2.39

- Add unique constraint on `source_id` and `cmms_integration_id` in CMMSTickets table

# v0.2.38

- Add `source_id` to the CMMS Tickets table

# v0.2.37

- Add get_project_data_timeseries_v2, polars_dataframe_async, and pivot_timeseries_by_tag_polars functions

# v0.2.36

- Change enumeration names for DefaultTimeRange, add two more

# v0.2.35

- Add `operational.om_contractor_scopes`

# v0.2.34

- Add crud functions for `DeviceModel` model

# v0.2.33

- Add `operational.unique_tag_patterns`

# v0.2.32

- Add new enum for device type PV_MODULE = 35

# v0.2.31

- Add `operational.device_models`

# v0.2.30

- Add `operational.custom_dashboards` and `operational.custom_dashboard_components`

# v0.2.29

- Added `cmms_tickets` table

# v0.2.28

- Add new equality comparisons for ModelList.find()
  - \_\_in (in [...])
  - \_\_ne ( != )
  - \_\_gt ( > )
  - \_\_lt ( < )
  - \_\_ge ( >= )
  - \_\_le ( <= )
- Add `get_maximum_event_id` to project crud
- Add `get_table_columns` to core utils

# v0.2.27

- Added a new enumeration for: device types, sensor types
- Added a way to check enumeration against database
- Centralized environment variables into settings.py which mirrors

# v0.2.26

- Add `device_type_id` to `sensor_types`
- Add `description` to `sensor_types` and `device_types`

# v0.2.25

- Switch psycopg2 from dev requirement to prod

# v0.2.24

- Update `admin.shared_pages.created_by` column to be a string

# v0.2.23

- add a shared pages table under admin

# v0.2.22

- Add `admin.company_secrets` to coordinate with AWS Secrets Manager

# v0.2.21

- Add multiple new date and interconnection fields to project model

# v0.2.20

- Add `get_data_timeseries_last_v2`

# v0.2.18

- Add `admin.user_kpi_types` table for tracking favorited KPIs

# v0.2.17

- Added `.find()` method to ModelList, added unit scaling to `core.core_utils.pivot_timeseries_by_tag()`

# v0.2.10

- Added `operational.calendar_item_assignments` table.

# v0.2.9

- Fix a SQL inefficiency related to `selectinload`

# v0.2.8

- Added `admin.teams` and `admin.team_members` tables.

# v0.2.5

- Make `capacity_bess_power_ac` and `capacity_bess_energy_bol_dc` columns in `operational.projects` non-nullable and default to `0`

# v0.2.4

- Make `address` column in `operational.projects` non-nullable

# v0.2.3

- Make `project_status_type_id` column in `operational.projects` non-nullable and default to `ONBOARDING`

# v0.2.2

- Add `capacity_energy_dc` column to `operational.projects` in `models.py`

# v0.2.1

- Remove deprecated columns from `operational.projects` in `models.py`
  - `capacity_power`
  - `duration`
  - `active`
  - `is_commissioning`
- Add multiple new date and interconnection fields to project model
