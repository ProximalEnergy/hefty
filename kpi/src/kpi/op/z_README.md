
# Service

This defines the primary non-domain user-facing classes and functions. It implements the classes
defined in base's protocol module which are used in `workflow`.

Depends on:
- `base`
- `domain`
- `infra`

Does not depend on:
- `workflow`

